[package]
name = "emoji-test"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { path = "../../repos/iced", default-features = false, features = [
    "wgpu", "tokio", "lazy", "advanced", "wayland", "x11",
] }
